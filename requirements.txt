fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Config
pydantic-settings==2.1.0

# Database
pymongo==4.6.0

# Audio processing (Stage 3+)
librosa==0.10.1
soundfile==0.12.1
numpy==1.26.2
scipy==1.11.4

# ML — Wav2Vec2 + PyTorch (Stage 4+)
# NOTE: torch is installed from the CPU wheel index (no CUDA on this host):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Verified working versions: torch 2.12.1+cpu, transformers 5.12.1.
# (The BACKEND_TECHNICAL_REFERENCE pins torch==2.10.0 / transformers==4.57.6,
#  which do not exist on PyPI — use current releases instead.)
torch
transformers

# Whisper (Stage 5+)
openai-whisper==20231117

# Vector store + Text embeddings (Stage 5+)
sentence-transformers==2.2.2
qdrant-client==1.7.0

# Redis cache (Stage 7+)
redis==5.0.1

# Training utilities (Stage 4+)
scikit-learn==1.3.2
joblib==1.3.2

# HTTP client for Ollama (Stage 5+)
httpx==0.25.2
requests==2.31.0
